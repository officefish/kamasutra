import {StyledSideItems} from "./styledSideItems";
import tw from "tailwind-styled-components";

/** Div which contains Logo svg */
export const StyledRightSideItems = tw(StyledSideItems)`
    px-3
    hover:bg-gray-100
    cursor-pointer
    `
